(function (angular, win) {
    'use strict';

    angular.module('app', [
        'app.controllers',
        'app.services'
    ]);

    angular.module('app.controllers', []);
    angular.module('app.services', []);
})(window.angular, window);
